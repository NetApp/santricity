# Generated by nar_santricity_inventory_builder
{%- if eseries_groups %}
  {%- set group_keys = (eseries_groups.keys() | list) %}
{%- else %}
  {%- set group_keys = [] %}
{%- endif %}
{%- if eseries_arrays %}
  {% set array_keys = (eseries_arrays.keys() | list) %}
{%- else %}
  {% set array_keys = [] %}
{%- endif %}


all:
  hosts:
  children:
    {{ eseries_group_name }}:
      children:
        {%- for index in range(array_keys | length) %}
        {%- if (loop.index0 % eseries_group_size) == 0 and group_keys %}

        {{ group_keys.pop(0) }}:
          hosts:
        {%- endif %}
          {%- if array_keys %}

            {{ array_keys.pop(0) }}:
          {%- endif %}
        {%- endfor %}